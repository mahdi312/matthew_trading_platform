<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- ── Console appender (standard output) ───────────────────── -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%15.15t] %-40.40logger{39} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Rolling file appender — errors/warnings in orange ASCII ── -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.trading-platform/logs/trading-platform.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.trading-platform/logs/trading-platform.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Error-only appender with ORANGE marker for UI-suppressed errors ── -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.trading-platform/logs/errors.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.trading-platform/logs/errors.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <!-- Orange color prefix for all WARN/ERROR entries -->
            <pattern>[ORANGE] %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Suppress noisy Hibernate / HikariCP internals ───────── -->
    <logger name="org.hibernate.engine.jdbc.spi.SqlExceptionHelper" level="ERROR" additivity="false">
        <appender-ref ref="ERROR_FILE"/>
    </logger>
    <logger name="org.hibernate.AssertionFailure" level="ERROR" additivity="false">
        <appender-ref ref="ERROR_FILE"/>
    </logger>
    <logger name="com.zaxxer.hikari" level="WARN" additivity="false">
        <appender-ref ref="ERROR_FILE"/>
    </logger>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.springframework.orm" level="WARN"/>

    <!-- ── Application root ─────────────────────────────────────── -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
