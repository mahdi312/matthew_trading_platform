<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mst.matt</groupId>
        <artifactId>trading-platform-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>notification-service</artifactId>
    <packaging>jar</packaging>
    <name>notification-service</name>
    <description>
        Kafka-driven notification fan-out service (Step 8 of the migration guide).
        Consumes alerts.triggered (from alert-service) and trades.executed /
        trades.closed (from trading-service) topics, then delivers real email
        (Spring JavaMail / SMTP) and Telegram (rubenlagus TelegramBots) notifications.

        Implements the production versions of EmailNotificationChannel and
        TelegramNotificationChannel — the same NotificationChannel contract as the
        stubs previously in alert-service (those stubs are now replaced with
        Kafka-only publish in alert-service, per option (a) in the migration guide).
    </description>

    <properties>
        <telegrambots.version>6.9.7.1</telegrambots.version>
    </properties>

    <dependencies>

        <!-- ── Web (health endpoint, future admin REST) ──────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- ── Service discovery ────────────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
        </dependency>

        <!-- ── Shared contracts (AlertTriggeredEventDto, TradeEventDto,
               NotificationChannel, UserPreferencesDto) ──────────────────── -->
        <dependency>
            <groupId>com.mst.matt</groupId>
            <artifactId>contracts</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- ── Kafka consumer (alerts.triggered + trades.executed/closed) ──
             spring-kafka brings both the consumer and KafkaTemplate (producer).
             Version managed by Spring Boot BOM (spring-kafka). ─────────── -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>

        <!-- ── Email (SMTP via JavaMailSender) ──────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <!-- ── Telegram Bot API (rubenlagus TelegramBots library) ─────────
             telegrambots-spring-boot-starter auto-registers long-polling bots
             as Spring beans when telegram.bot.enabled=true.
             Pin to 6.8.0 — same major as the desktop monolith's bot; the API
             shape of TelegramLongPollingBot / SendMessage is stable.
             TelegramBots 7.x+ moves to a different package structure — update
             together with desktop when you do the Step 12 thin-client pass. ──── -->
        <dependency>
            <groupId>org.telegram</groupId>
            <artifactId>telegrambots-spring-boot-starter</artifactId>
            <version>${telegrambots.version}</version>
        </dependency>

        <!-- ── Actuator (health, info) ───────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- ── Configuration-processor ───────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ── Lombok ───────────────────────────────────────────────────── -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ── Test ─────────────────────────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <finalName>notification-service</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
